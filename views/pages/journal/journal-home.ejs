<!DOCTYPE html>
<html>

<head>
    <title>My Journals</title>
    <%- include ("../../partials/header.ejs") %>

</head>

<body>

    <%- include ("../../partials/nav.ejs") %>

    <!-- <div class="container">
        <h2>Journal Entries</h2>

        <ul>
            for each of the object in results
            <% journals.forEach(function(r) { %>
            <li> id: <%= r.user_id %> - date: <%= r.date %> - text: <%= r.journal %> </li>
            <% }); %>
        </ul>
        <a href="/journal/create">Add new entry</a>
    </div> -->

    <div class="container">
        <h1 class="">Journal Entries</h1>
        <a href="/journal/create" class="btn btn-success">New Journal</a>

        <% journals.forEach(journal => { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title"><%= journal.title %></h4>
                <div class="card-subtitle text-muted md-2">
                    <%= journal.date.toLocaleDateString() %>
                </div>
                <div class="card-text mb-2">
                    <%= journal.description%>
                </div>
                <div>
                    <a href="/journal/<%= journal.journal_id%>" class="btn btn-primary">Read More</a>
                    <a href="/journal/edit/<%= journal.journal_id%>" class="btn btn-info">Edit</a>
                    <form action="journal/delete/<%= journal.journal_id%>?_method=DELETE" method="POST"
                        class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        </div>
        <% }) %>
    </div>

</body>

</html>